pipeline {
    environment {
        imagename = "paivae/mundose-web"
        dockerImage = ''
        containerName = 'mundose-webapp-test'
        dockerHubCredentials = 'DockerHubs'
    }
    agent any
    stages {


        stage('Deploy Image') {
            steps {
                sshagent(credentials: ['webapp-prod-server']) {
                    sh 'ssh -o StrictHostKeyChecking=no -l jenkinsadmin web.mundose.paivae.com uname -a'
                }
            }
        }
    }   

    post {
    always {
      sh 'docker logout'
      echo 'Session cerrada de docker'
    }
  }
}

